__pycache__/
*.pyc

# Local model weights, split files, and build/source checkouts are large runtime state.
models/
src/

# Generated outputs and run logs live outside git.
outputs/
logs/

# LoRA weights are local model assets. Keep the manifest tracked.
loras/**/*.safetensors
loras/**/*.pt
loras/**/*.gguf
