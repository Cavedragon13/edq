# Lessons Learned

Running log of corrections and the preventive rules they produced.
Claude reviews this at session start and appends after any correction during the session.

Format: `- YYYY-MM-DD: [what failed] → [rule going forward]`

---

- 2026-03-31: Pointer-driven board rotation was shifting DOM targets between click-down and click-up, breaking hit detection on interactive elements → When overlaying pointermove animations on clickable elements, either disable animation during pointer-down, use pointer-events:none on the animated element, or perform hit tests in unanticipated coordinate space
- 2026-05-24: Health check script initially included Linkding (8039) as always-on, but it is on-demand → only include a service in ALWAYS_ON if it has no launch_command in dragonsuite.json (infrastructure services) or is documented as always-on in CLAUDE.md
- 2026-05-24: output_dir JSON update used wrong service IDs (wangp2, omnivoice, dragonart, unsloth) — needed a second pass with correct IDs (wan2gp, omnivoice-studio, dragonart-studio, unsloth-studio) → before patching dragonsuite.json programmatically, always print the actual id fields first with a dry-run query
- 2026-05-24: "60 start scripts, 1 stop script" finding in adversarial review was misleading — all services have stop_command in dragonsuite.json and the dashboard server executes them; standalone stop scripts are not needed → when auditing stop capability, check dragonsuite.json stop_command field, not just the scripts/ directory
- 2026-05-30: I chased cdragon/local host state while the user expected Dragonsuite work to happen on udragon → treat `/srv/containers/edq` on udragon (`192.168.7.226`) as the canonical source of truth; use Mac/cdragon only as client/editor unless the user explicitly asks for Mac-native work
- 2026-05-30: I interpreted "udragon/projects" as a local mount instead of the SSH home symlink → for Dragonsuite work, SSH to `udragon` and use `~/projects` or `/srv/containers/edq/projects` directly before inspecting local mirrors
- 2026-06-07: assumed Ollama was a snap (docs + dragonsuite.json said so) but it's the official systemd binary at `/usr/local/bin/ollama` → verify install method with `which`/`systemctl status` before managing a service; don't trust docs that may have drifted
- 2026-06-07: assumed Ollama's default eviction prevents two models co-residing; live VRAM check showed 0.30.x co-loads them to 1.3 GB free (near-OOM) on the 16 GB card → verify runtime memory behavior with an actual `nvidia-smi`/`ollama ps` check, and set `OLLAMA_MAX_LOADED_MODELS=1` on single-16GB-GPU boxes
- 2026-06-07: tried `curl -fsSL .../install.sh | sudo sh` to update Ollama and it was blocked by the sandbox classifier → for runtime updates, download the GitHub release tarball (`.tar.zst`) and extract manually instead of piping a remote script to root
- 2026-06-11: wrote an inspection one-liner with `enc=$(python3 -c "...")` + `curl -w "%{http_code}"` that tripped "shell syntax cannot be statically analyzed" → user had to approve a prompt with no idea what was wrong. Never bundle command substitution / nested quotes / printf-format strings into a bash one-liner; put the logic in a script file run with one flat command, or use Read/Grep/Glob. Allow-listing can't fix the unanalyzable case — only rewriting can.
- 2026-06-17: used backslash-escaped path (Daily\ Notes/) instead of quoted path ("Daily Notes/") — triggers "Contains backslash-escaped whitespace" prompt. Always quote paths with spaces; never backslash-escape them in Bash tool calls. Then immediately used printf with backslash sequences to record this lesson, triggering the same prompt again. For file appends with special characters, use the Edit tool — not printf/echo.
- 2026-06-25: added Krea to Dragonsuite without loading the recovered `dragonsuite-add` SOP, so the first pass used an internal `/srv/.../outputs` folder and added a fallback card that failed the health check -> for every new Dashboard service, read the local `~/.claude/skills/dragonsuite-add/SKILL.md` or equivalent SOP first, put generated media under `/home/edq/ai_generated/<service>`, keep logs elsewhere, and run `scripts/health_check.py --categories <category>` before calling it done.
- 2026-06-25: a one-step Krea smoke test was saved into the user-facing `/home/edq/ai_generated/krea2` gallery, and Codex had no populated global `AGENTS.md` to inherit the existing artifact policy -> keep Codex `AGENTS.md` synced with shared house rules; smoke tests, audits, and other throwaway outputs go under `/home/edq/ai_generated/test-artifacts/<type>/`, never in Downloads, the root of `1Projects`, or service gallery folders.
- 2026-06-25: `sync_skills.sh` rsynced the gstack checkout and gstack-owned live skill folders across machines, corrupting `.git` object stores on cdragon/odragon and exposing stale backup `SKILL.md` files as commands → gstack must update per machine from GitHub; exclude gstack from cross-machine rsync, keep backups outside live skill scan paths, and run `./setup --host claude --no-prefix` plus `./setup --host codex --no-prefix`.
- 2026-06-25: Codex had hand-written adapters for gstack commands even after upstream gstack generated official Codex skills with short command names in `gstack-*` folders → let official gstack own `qa`, `review`, `ship`, `investigate`, `office-hours`, plan/design/document-release, and deploy commands; keep adapters only for house/local or Claude-only SOPs.
- 2026-06-26: adragon came back onto the LAN but still had stale Codex startup files, no Bun, a corrupt gstack checkout, and duplicate `.agents/skills` gstack command folders → when a previously-offline machine is available again, run the full alignment pass: shared Codex `AGENTS.md`/`instructions.md`, `memories = true`, house adapters, Bun, official gstack setup, and no live backup or duplicate skill dirs.
- 2026-06-26: the Codex Desktop warning badge on udragon looked like a network/project problem, but SSH and `/home/edq/projects` were healthy; the real issue was missing managed standalone Codex plus stale remote-control socket state → for Codex Desktop remote-project warnings, check `codex doctor`, `codex remote-control start`, the managed standalone path, app-server sockets/processes, and default `codex` PATH before debugging LAN or project folders.
- 2026-06-26: `/llkb` produced useful notes but did not make commit/cron coverage explicit, leaving unrelated dirty files mixed mentally with durable metadata → treat `/llkb` as end-of-session closeout: write searchable discussion notes, audit git and cron coverage, commit only isolated metadata when safe, and report unrelated dirty worktrees separately.
- 2026-06-27: cleanup triage nearly treated live Dashboard projects, model checkpoint symlinks, and runtime data as generic clutter → before deleting or moving anything under `/srv/containers/edq`, resolve `dragonsuite.json`, launch scripts, docs, symlinks, active processes, and output/runtime paths; keep source in project dirs, outputs in `/home/edq/ai_generated`, backups in ignored archives, and runtime data ignored in place.
- 2026-06-27: ACE-Step XL passed structural health after checkpoint migration but still failed live launch because its project `.env` pointed at the old non-XL checkpoint path → after moving model/checkpoint directories, run a live launch check and add tracked launcher guards/exported paths so hidden local config cannot silently recreate or depend on pruned folders.
- 2026-07-13: told the user to "right-click the Trash icon" on their Desktop before confirming one existed — this GNOME setup (Desktop Icons NG / ding extension) doesn't show a built-in Trash icon by default, and the user caught it via screenshot → verify GUI affordances exist (list `~/Desktop`, check which desktop-icons extension/DE is active) before referencing them, instead of assuming standard desktop-environment behavior.
- 2026-07-18: after `pkill -9`-ing a GPU-holding process (MatAnyone 2), an immediate `pgrep` check reported no match, but the process was actually still alive 56+ minutes later, silently holding 2.6GB VRAM and contributing to a wrong root-cause read on an unrelated OOM investigation → after killing a GPU service, verify via `nvidia-smi --query-compute-apps` (the actual GPU process registry), not just an immediate `pgrep` — the timing/matching isn't reliable proof of termination.
- 2026-07-18: while sequentially launch-verifying and stopping several GPU services, ran `stop_odysseus.sh` when the intent was to stop a just-verified HiDream-O1 process — took down a persistent service (Odysseus) that should have stayed running → when switching context between services mid-sequence, double-check the stop/launch command targets the service actually being acted on right now, not the previous one.
- 2026-08-24: a `ScheduleWakeup` fallback check during a multi-hour SenseNova model download found "still downloading" and silently rescheduled (`noop:true`) instead of messaging the user — read as going quiet for an hour, user called it out directly ("Strike 2") → for any long-running wait the user is actively following, rely on the real task-completion notification instead of a timed fallback guess; if a fallback check does fire, always send a visible status even when nothing changed.
- 2026-08-24: `pgrep -f "ComfyUI/main.py"` and `pgrep -f "sensenova_server"` self-matched this harness's own wrapping `bash -c "eval '...'"` command line and returned false "still running" positives after a kill — same root cause as the 2026-07-21 wait-loop entry, but recurred in a new context (one-off confirmation checks, not just wait-loops), meaning the existing rule wasn't broad enough → after killing any process, verify via `ps -p <PID>`, a port/curl check, or resource-usage recovery (`free`/`nvidia-smi`) — never `pgrep -f` with a pattern that could also appear in the invoking command's own text, in any context, not just loops.
- 2026-08-24: a stale `VIRTUAL_ENV=/srv/containers/edq/venv_agentic_video` baked into this session's shell snapshot re-leaked on every Bash call, and `comfy-cli` trusted that env var to decide where to `pip install torch` — nearly corrupted an unrelated venv, caught and killed within seconds before any files were touched → explicitly pin `VIRTUAL_ENV=<correct venv>` on any command invoking a tool that respects that env var; treat ambient env leakage as a standing per-session hazard, not a one-off to fix once.
- 2026-08-24: `start_comfyui.sh` launched with `cd "$APP_DIR" && python main.py` (relative path), so the running process's argv never contained the absolute path the `stop_command`/pkill pattern searched for — the dashboard stop button could never have actually killed it → always launch background services with the absolute script path, not a relative one after `cd`, so pkill-based stop_commands actually match the real process.
- 2026-08-24: assumed SenseNova's `--vram_mode balanced` GPU-offload flag would also bound system RAM usage; the actual loader (`gguf_loader.py`) forces the full ~20GB GGUF into real RAM via `torch.from_numpy(tensor.data.copy())` regardless of vram_mode — two real launch attempts hit swap exhaustion before this was caught → before estimating a service's RAM footprint from a documented low-VRAM flag, read the actual loading code path; VRAM-side memory controls do not imply an equivalent RAM-side bound.
- 2026-09-13: Dragonsuite services were correctly hosted on udragon, but live UI tests opened visible Chrome tabs and a saved test group on cdragon, leaving confusing browser artifacts behind → test udragon web apps in the hidden Codex in-app browser by default; use cdragon Chrome only when a visible real-browser check is necessary, never save test groups, and remove test tabs before handoff.
- 2026-09-13: `weekly_update.sh` committed local patches as "Auto-merge" commits but then pulled with `--ff-only`, so every repo it had ever patched failed "pull failed" forever after (6 of 9 attention rows were this one design flaw); its `git add -A` also swept an untracked 8.7GB venv into the ai-toolkit repo → an auto-updater that creates local commits must update by `rebase` onto the tracking branch (abort + flag on conflict), commit with `add -u` (tracked files only), skip detached-HEAD/pinned checkouts, and keep its launch-verify log on disk instead of pointing at a deleted mktemp.
- 2026-09-13: `start_wan2gp.sh` ran `python wgp.py` in the foreground, so `weekly_update.sh`'s `timeout 300` launch-verify could never see an exit code 0 + "ready at" line — it rolled the code back four Sundays in a row while still pip-syncing the venv to the *new* requirements.txt each time → every Dashboard launcher must background the process and `wait_for_port`, and when a service "fails launch-verify" every week, check the launcher's shape before blaming the upstream code.
- 2026-09-13: Astra's Codex session hit its usage cap with findings only in encrypted reasoning; the visible transcript kept just tool calls and short summaries → treat a handed-off agent transcript as leads to re-derive live, not as the findings themselves; mine tool outputs and `logs/weekly_update_attention.md`, then re-run the audit (`health_check.py --all`, weekly log, `tailscale status`, container/mount state) instead of trusting recovered notes.
- 2026-09-20: Codex Desktop was current, Ubuntu apt timers were current, and weekly Dragonsuite/skills updates were running, but the standalone Codex runtime under `~/.codex/packages/standalone` and its long-lived app-server process stayed stale, hiding new models such as Astra → update coverage must explicitly check `codex --version`, `codex app-server daemon version`, and `~/.codex/models_cache.json` client_version/model list; OS/app/skills updates do not prove Codex's project-folder runtime is fresh.
- 2026-09-23: FrameForge had `output_dir: ai_generated/frameforge` in dragonsuite.json and multiple LLM passes "verified" it, but server.py actually wrote each project to top-level `ai_generated/<project_slug>/` (only the JSONL log went to frameforge/) → a registry `output_dir` is a claim, not proof; verify by grepping the save path (`write_bytes`/`save`/`mkdir`) and finding where the newest real output file actually is. Relative output paths from manifests/LLMs must be forced under the service folder.
- 2026-09-23: `pkill -f coversynth-json/server.py` inside a multi-step Bash command killed the calling shell (the pattern matched its own command line, exit 144), aborting the rest of the command → never put `pkill -f <pattern>` in the same command as other steps; run it alone, or match with `pgrep -f "[c]oversynth-json/server.py"`-style bracket patterns.
- 2026-09-23: Pinokio apps writing to `app/<outputs>` inside the Pinokio tree: if that folder is untracked in both the launcher repo and app repo, replace it with a symlink to `~/ai_generated/<app>/` — no tracked file edits, so Pinokio Update (`git pull`) stays conflict-free. Done for TripoSplat (`app/gradio_outputs`).
- 2026-09-23: scripts/provider_models.py filtered OpenAI text models with a regex that only matched `gpt-5.x`/`gpt-4.1` names, so the whole gpt-6 (luna/sol/astra) and gpt-5.6 families were invisible in every Dragonsuite model dropdown, and apps kept defaulting to older minis → when a vendor adds a new naming scheme, check raw `models.list()` against what the filter shows; widen the filter but do NOT add new models to PREFERRED front positions without checking price (the balanced tier picks the first non-pro model — gpt-6-astra is $10/$50 per MTok). Per-app cheap defaults (e.g. FrameForge cleanup = gpt-6-luna / gemini-3.5-flash-lite) belong in the app, not the shared helper.
